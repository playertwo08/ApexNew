"use client";
import{createContext,useContext,useEffect,useMemo,useState,type ReactNode}from"react";import type{CartItem,Product}from"@/lib/types";
type CartContextValue={items:CartItem[];count:number;subtotal:number;addItem:(product:Product,selections?:Record<string,string>,quantity?:number)=>void;updateQuantity:(key:string,quantity:number)=>void;removeItem:(key:string)=>void;clearCart:()=>void};
const CartContext=createContext<CartContextValue|null>(null);const STORAGE_KEY="apex-sports-cart";
export function CartProvider({children}:{children:ReactNode}){const[items,setItems]=useState<CartItem[]>([]);const[ready,setReady]=useState(false);useEffect(()=>{try{const saved=localStorage.getItem(STORAGE_KEY);if(saved)setItems(JSON.parse(saved))}catch{}setReady(true)},[]);useEffect(()=>{if(ready)localStorage.setItem(STORAGE_KEY,JSON.stringify(items))},[items,ready]);const addItem=(product:Product,selections:Record<string,string>={},quantity=1)=>{const key=`${product.slug}:${Object.entries(selections).sort().map(([k,v])=>`${k}-${v}`).join("|")}`;setItems(current=>{const found=current.find(item=>item.key===key);return found?current.map(item=>item.key===key?{...item,quantity:item.quantity+quantity}:item):[...current,{product,selections,quantity,key}]})};const value=useMemo(()=>({items,count:items.reduce((sum,item)=>sum+item.quantity,0),subtotal:items.reduce((sum,item)=>sum+item.product.price*item.quantity,0),addItem,updateQuantity:(key:string,quantity:number)=>setItems(current=>quantity<1?current.filter(item=>item.key!==key):current.map(item=>item.key===key?{...item,quantity}:item)),removeItem:(key:string)=>setItems(current=>current.filter(item=>item.key!==key)),clearCart:()=>setItems([])}),[items]);return <CartContext.Provider value={value}>{children}</CartContext.Provider>}
export function useCart(){const context=useContext(CartContext);if(!context)throw new Error("useCart must be used within CartProvider");return context}
