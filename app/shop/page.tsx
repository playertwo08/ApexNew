import type{Metadata}from"next";import{ShopClient}from"@/components/shop-client";export const metadata:Metadata={title:"Shop"};export default async function ShopPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const p=await searchParams;return <main id="main-content"><ShopClient initialQuery={typeof p.q==="string"?p.q:""} initialCategory={typeof p.category==="string"?p.category:""} initialSort={typeof p.sort==="string"?p.sort:"featured"}/></main>}
